\section{FITTING METHODOLOGIES AND STABILITY ANALYSIS}
\label{sec:fitting_methodologies}

An initial survey of the extracted lattice data reveals a striking disparity in statistical precision among the invariant amplitudes. The real part of the unpolarized amplitude, $\tAmp_{2B}^{\text{Re}}$, exhibits remarkably small statistical uncertainties across the entirety of the simulated phase space. In contrast, the imaginary unpolarized amplitude $\tAmp_{2B}^{\text{Im}}$ and the real Sivers amplitude $\tAmp_{12B}^{\text{Re}}$ suffer from comparatively larger gauge noise, while the imaginary Sivers amplitude $\tAmp_{12B}^{\text{Im}}$ has already been discarded due to its statistical consistency with zero. Because $\tAmp_{2B}^{\text{Re}}$ constitutes the leading-twist unpolarized background and possesses the highest data quality, establishing a robust, highly stable global fit for this specific amplitude serves as the foundational core of our entire numerical analysis. 

A central methodological decision in this analysis is the choice of domain for these global fits. While it may seem intuitive to fit the amplitudes directly in the Lorentz-invariant phase space spanned by $(P \cdot b, b^2)$, doing so introduces severe numerical and physical instabilities. Instead, performing the parameterizations natively in the Cartesian spatial domain $(b_L, b_T)$ is demonstrably superior for several reasons.

First, fitting directly in $(b_L, b_T)$ provides access to a significantly larger, more uniformly distributed set of discrete data points. More importantly, it resolves severe kinematic degeneracies that are otherwise hidden in the invariant space. The squared separation distance, defined as $b^2 = b_L^2 + b_T^2$, is strictly non-negative. Because of this circular symmetry in the definition, multiple distinct combinations of longitudinal and transverse separations (e.g., a large $b_L$ with a small $b_T$, versus a small $b_L$ with a large $b_T$) can map to the exact same invariant $b^2$. 

If the fits were performed blindly in the $(P \cdot b, b^2)$ basis, these distinct orthogonal spatial dependencies would be artificially compressed and entangled into a single coordinate. By fitting the two-dimensional surface directly over the $(b_L, b_T)$ grid, we allow the model to independently resolve the longitudinal decay profile separately from the transverse decay profile. Once this high-fidelity spatial surface is successfully modeled and stabilized using the precision $\tAmp_{2B}^{\text{Re}}$ data, the continuous parameterization can then be analytically evaluated at any invariant kinematic point to perform the final Fourier transforms.

\subsection{Coordinate Transformation and the Simple Gaussian Model}
\label{sec:gaussian_transformation}

As motivated in the previous section, we perform our parameterizations natively in the Cartesian $(b_L, b_T)$ lattice space to resolve kinematic degeneracies, and subsequently map the resulting functional forms into the continuous Lorentz-invariant $(P \cdot b, b^2)$ phase space. To establish a baseline for our stability analysis, we first test a simple two-dimensional Gaussian profile.

For the real parts of the amplitudes ($\tAmp^{Re}_{iB}$), the fundamental Hermitian conjugation symmetries dictate that the function must be strictly even in the longitudinal separation $b_L$. We therefore model the real amplitude with a standard 2D Gaussian centered at the origin:
\begin{align}
    \tAmp^{Re}_{iB}&=\gamma \exp{\left[-\alpha \textcolor{red}{b_{L}^{2}}-\beta \textcolor{red}{b_{T}^{2}}\right]}
\end{align}
To transform this into the invariant phase space, we must eliminate the transverse coordinate $b_T$. We utilize the geometric constraint defining the squared separation vector, $\textcolor{blue}{b}^2 = \textcolor{red}{b_L^2} + \textcolor{red}{b_T^2}$, which allows us to substitute $\textcolor{red}{b_T^2} = \textcolor{blue}{b}^2 - \textcolor{red}{b_L^2}$:
\begin{align}
    \tAmp^{Re}_{iB}&=\gamma \exp{\left[-\alpha \textcolor{red}{b_{L}^{2}}-\beta \left(\textcolor{blue}{b}^{2}-\textcolor{red}{b_{L}^{2}}\right)\right]}
\end{align}
Grouping the terms by the longitudinal coordinate isolates the dependence on the invariant squared separation $\textcolor{blue}{b}^2$:
\begin{align}
    \tAmp^{Re}_{iB}&=\gamma \exp{\left[-\left(\alpha-\beta\right) \textcolor{red}{b_{L}^{2}}-\beta \textcolor{blue}{b}^{2}\right]}
\end{align}
Finally, we apply the Lorentz-invariant projection derived in Section \ref{sec:lorentz_mapping}, where the longitudinal spatial coordinate is given by $b_L = (\textcolor{blue}{P \cdot b}) / (-P_L)$. Squaring this substitution completely maps the amplitude into the required invariant frame:


\begin{align}
   \tAmp^{Re}_{iB} &=\gamma \exp{\left[-\frac{\left(\alpha-\beta\right)}{P_{L}^2} (\textcolor{blue}{P\cdot b})^{2}-\beta \textcolor{blue}{b}^{2}\right]}
\end{align}
\begin{figure}[H]
    \centering
    % Left Column: Re A2B
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{plots/Gaussian_fit_all_b/Gaussian_A2B_Re_P1_FailedFit.pdf}\\[2ex]
        \includegraphics[width=\linewidth]{plots/Gaussian_fit_all_b/Gaussian_A2B_Re_P1_FailedFit_details1.pdf}\\[2ex]
        \includegraphics[width=\linewidth]{plots/Gaussian_fit_all_b/Gaussian_A2B_Re_P1_FailedFit_details2.pdf}
    \end{minipage}\hfill
    % Right Column: Re A12B
    \begin{minipage}{0.48\textwidth}
        \centering
        \includegraphics[width=\linewidth]{plots/Gaussian_fit_all_b/Gaussian_A12B_Re_P1_FailedFit.pdf}\\[2ex]
        \includegraphics[width=\linewidth]{plots/Gaussian_fit_all_b/Gaussian_A12B_Re_P1_FailedFit_details1.pdf}\\[2ex]
        \includegraphics[width=\linewidth]{plots/Gaussian_fit_all_b/Gaussian_A12B_Re_P1_FailedFit_details2.pdf}
    \end{minipage}
    
    \caption{Failure of Gaussian model fit for $\tAmp^{Re}_{2B}$ (with $\alpha-\beta = -0.0427(27)$ and $\chi^2/\text{dof}\approx 235$) and $\tAmp^{Re}_{12B}$ (with $\alpha-\beta = -0.022(25)$ and$\chi^2/\text{dof}\approx 1.4$) amplitudes.}
    \label{fig:gaussian_failure}
\end{figure}

We apply an analogous transformation to the imaginary amplitudes ($\tAmp^{Im}_{iB}$). Because the imaginary amplitudes must be strictly odd functions of $b_L$, the Gaussian profile must be multiplied by a linear prefactor of $b_L$:
\begin{align}
    \tAmp^{Im}_{iB}&=\gamma \textcolor{red}{b_{L}}\exp{\left[-\alpha \textcolor{red}{b_{L}^{2}}-\beta \textcolor{red}{b_{T}^{2}}\right]}
\end{align}
Once again, we substitute the transverse component using the geometric constraint $\textcolor{red}{b_T^2} = \textcolor{blue}{b}^2 - \textcolor{red}{b_L^2}$:
\begin{align}
    \tAmp^{Im}_{iB}&=\gamma \textcolor{red}{b_{L}}\exp{\left[-\alpha \textcolor{red}{b_{L}^{2}}-\beta \left(\textcolor{blue}{b}^{2}-\textcolor{red}{b_{L}^{2}}\right)\right]}
\end{align}
Factoring the exponent isolates the invariant $b^2$ dependence from the longitudinal decay profile:
\begin{align}
    \tAmp^{Im}_{iB}&=\gamma \textcolor{red}{b_{L}}\exp{\left[-\left(\alpha-\beta\right) \textcolor{red}{b_{L}^{2}}-\beta \textcolor{blue}{b}^{2}\right]}
\end{align}
Substituting $\textcolor{red}{b_L} = (\textcolor{blue}{P \cdot b}) / (-P_L)$ for both the linear prefactor and the squared exponent yields the final invariant imaginary amplitude:
\begin{align}
    \tAmp^{Im}_{iB} &=\frac{\gamma}{-P_{L}} (\textcolor{blue}{P\cdot b}) \exp{\left[-\frac{\left(\alpha-\beta\right)}{P_{L}^2} (\textcolor{blue}{P\cdot b})^{2}-\beta \textcolor{blue}{b}^{2}\right]}
\end{align}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.55\textwidth]{plots/Gaussian_fit_all_b/Gaussian_A2B_Im_P1_FailedFit.pdf}\\[2ex]
    \includegraphics[width=0.55\textwidth]{plots/Gaussian_fit_all_b/Gaussian_A2B_Im_P1_FailedFit_details1.pdf}\\[2ex]
    \includegraphics[width=0.55\textwidth]{plots/Gaussian_fit_all_b/Gaussian_A2B_Im_P1_FailedFit_details2.pdf}
    
    \caption{Failure of the simple Gaussian model fit for the imaginary $\tAmp^{Im}_{2B}$ (with $\alpha-\beta = 0.008\pm0.017$ and$\chi^2/\text{dof}\approx 2.9$) amplitude.}
    \label{fig:gaussian_failure_ImA2B}
\end{figure}

\subsubsection{Pathology of the Simple Gaussian Model}
While the simple Gaussian parameterization provides a mathematically clean transformation into the $(P \cdot b, b^2)$ phase space, it ultimately fails to satisfy the rigorous physical constraints required to extract the Sivers shift.

The fundamental objective of mapping these amplitudes into the invariant space is to perform a continuous Fourier transform over $P \cdot b$ to extract the momentum fraction $x$-dependence. Looking at the transformed real amplitude, the exponential decay governing the $P \cdot b$ integration is parameterized by:
\begin{equation}
    \exp{\left[-\frac{\left(\alpha-\beta\right)}{P_{L}^2} (\textcolor{blue}{P\cdot b})^{2} \right]}
\end{equation}
For the infinite Fourier integral $\int d(P\cdot b) e^{ix(P\cdot b)} \tAmp_{iB}$ to converge, the argument of the exponential must remain negative as $(P \cdot b) \to \pm \infty$. This imposes a strict mathematical bound on our fit parameters: the condition $(\alpha - \beta) > 0$ must hold. If $\alpha < \beta$, the coefficient becomes positive, leading to exponential growth of the amplitude at large longitudinal separations and rendering the integral physically divergent.

Unfortunately, applying this simple Gaussian fit to our high-precision lattice data reveals a critical failure in the model. When fitting the unpolarized real amplitude, $\tAmp^{Re}_{2B}$, the parameter extraction consistently yields $\alpha < \beta$. This implies that the amplitude decays faster in the transverse direction than in the longitudinal direction, leading to an unphysical, non-integrable expansion when mapped into the $(P \cdot b, b^2)$ invariant space. 

Furthermore, the overall goodness-of-fit for the simple Gaussian ansatz is exceptionally poor. The resulting $\chi^2/\text{dof}$ is unacceptably high ($\chi^2/\text{dof}\approx 230$), indicating that a single, symmetric 2D Gaussian decay profile is insufficiently flexible to capture the true underlying physics of the non-local operator. 

To resolve these unphysical divergences and properly model the lattice correlation functions, we are forced to abandon the simple Gaussian model in favor of more sophisticated, multi-parameter functional forms that can accommodate the distinct decay rates while guaranteeing convergence in the Fourier domain.

\subsection{Physics-Informed Symbolic Regression via PySR}
\label{sec:pysr_methodology}

The failure of the simple Gaussian ansatz highlights a fundamental challenge in phenomenological lattice QCD: guessing the correct functional form of a non-local amplitude in a multi-dimensional space is highly susceptible to human bias and unphysical rigidities. To overcome this, we abandon presumed functional forms and instead employ Symbolic Regression (SR). 

Unlike traditional regression, which optimizes the numerical parameters of a pre-determined function, symbolic regression simultaneously searches the space of mathematical expressions and their parameters. It aims to discover the analytical equation that best balances accuracy (goodness-of-fit) with simplicity (parsimony). To perform this high-dimensional search, we utilize \texttt{PySR} \cite{Cranmer2023pysr}, a highly optimized framework that leverages Genetic Programming to discover interpretable physical laws from data.

\subsubsection{Genetic Algorithms and Expression Trees}
At the core of \texttt{PySR} is a genetic algorithm \cite{Koza1992GP, Schmidt2009Distilling}. In this paradigm, mathematical equations are represented as binary trees, where internal nodes are mathematical operators (e.g., $+$, $\times$, $\exp$, $\sin$) and leaf nodes are variables (e.g., $b_L, b_T$) or numerical constants. 

The algorithm mimics biological evolution. It begins by initializing a random "population" of these equation trees. The performance of each tree is evaluated against the lattice data using a specified loss function, and it is assigned a "fitness" score. The best-performing equations survive and undergo evolutionary operations to produce the next generation. These operations include:
\begin{itemize}
    \item \textbf{Crossover:} Sub-trees from two highly fit parent equations are swapped to create offspring.
    \item \textbf{Mutation:} Random alterations are applied to a tree, such as changing an operator (e.g., $+$ to $\times$), perturbing a constant, or appending a new mathematical branch.
\end{itemize}
This cycle repeats for thousands of generations, iteratively distilling the population toward expressions that are both accurate and mathematically concise. 

\begin{figure}[htpb]
    \centering
    \begin{tikzpicture}[
        node distance=1.5cm and 2cm,
        box/.style={draw, rectangle, rounded corners, minimum width=3cm, minimum height=1cm, align=center, fill=blue!5},
        arrow/.style={->, >=stealth, thick}
        ]
        
        % Nodes
        \node[box] (init) {Initialize Population \\ (Random Equation Trees)};
        \node[box, below=of init] (eval) {Evaluate Fitness \\ (Compute Custom Loss)};
        \node[box, below=of eval] (select) {Selection \\ (Keep fittest equations)};
        \node[box, right=of select] (evolve) {Evolution \\ (Crossover \& Mutation)};
        \node[box, right=of init] (pareto) {Pareto Front \\ (Best Accuracy vs. Complexity)};
        
        % Arrows
        \draw[arrow] (init) -- (eval);
        \draw[arrow] (eval) -- (select);
        \draw[arrow] (select) -- (evolve);
        \draw[arrow] (evolve) |- (eval);
        \draw[arrow] (select) -| (pareto);
        
    \end{tikzpicture}
    \caption{Flowchart illustrating the evolutionary loop of the Genetic Algorithm utilized by \texttt{PySR} to discover analytical equations. The process continuously optimizes the population of mathematical expressions against the custom physics-informed loss function.}
    \label{fig:pysr_flowchart}
\end{figure}

The specific configuration of the \texttt{PySR} search, including the permitted mathematical operators, nesting constraints, and key hyperparameters, is detailed in Table~\ref{tab:pysr_config}. Notably, the operator library includes a custom Lorentzian-type binary operator, $\text{lor}(x, y) = 1/(1 + x^2 + y^2)^2$, alongside standard arithmetic and exponential functions, while nesting constraints prevent the algorithm from generating physically meaningless double-exponential or double-decay structures.

\input{PySR_outputs/pysr_model_config}


\subsubsection{The Physics-Informed Custom Loss Function}

While standard symbolic regression optimizes a standard metric like Mean Squared Error (MSE), blindly applying it to our lattice data poses a serious risk. A genetic algorithm might discover a polynomial or diverging exponential function that fits the local, finite grid of $(b_L, b_T)$ data exceptionally well, achieving a low MSE. However, as demonstrated in Section \ref{sec:gaussian_transformation}, if the amplitude does not strictly decay as $b_L \to \infty$, the subsequent continuous Fourier transform over $P \cdot b$ will diverge, yielding unphysical infinite momentum distributions.

To explicitly force the genetic algorithm to respect the asymptotic physics of the nucleon, we implemented a physics-inspired custom loss function within \texttt{PySR}. We construct this loss function systematically. First, the base accuracy of the model is dictated by the standard reduced chi-squared statistic, ensuring the model respects the varying uncertainties of the discrete lattice measurements:
\begin{align}
    \mathcal{L}_{\text{base}} = \frac{\chi^2}{\text{dof}} = \frac{1}{N - k} \sum_{i=1}^{N} \left( \frac{\tAmp_{2B}^{\text{Re, lattice}}(i) - \tAmp_{2B}^{\text{Re, model}}(i)}{\sigma_i} \right)^2
\end{align}

Here, $N$ is the number of lattice data points, $k$ is the number of degrees of freedom in the generated expression, and $\sigma_i$ is the statistical error of the $i$-th lattice point.

To enforce the asymptotic decay required for Fourier integrability, we introduce a severe penalty term. We evaluate the proposed model equation at artificially large values of the longitudinal separation, $b_L^{\text{large}}$, while keeping the transverse separations $b_T$ fixed to physical slices. We compute the vector sum of the absolute model predictions in this extreme asymptotic regime:
\begin{align}
    \mathcal{P}_{\text{asymptotic}} = \sum_{b_T \in \{b_{T}^{\text{fixed}}\}} \sum_{b_L \in \{b_{L}^{\text{large}}\}} \left| \tAmp_{2B}^{\text{Re, model}}(b_L, b_T) \right|
\end{align}

If a genetically generated equation diverges or grows at large $b_L$, this penalty term $\mathcal{P}_{\text{asymptotic}}$ will explode, immediately ruining the equation's fitness score and causing it to be "killed off" in the next evolutionary generation. Conversely, if the equation naturally decays toward zero as physics demands, the penalty safely vanishes.

Finally, we combine these terms into the total objective function utilized by \texttt{PySR}. To strictly enforce the asymptotic behavior without overwhelming the base goodness-of-fit during the evolutionary search, the penalty is scaled by a small regularization parameter $\epsilon$:
\begin{align}
    \mathcal{L}_{\text{custom}} &= \mathcal{L}_{\text{base}} + \epsilon \, \mathcal{P}_{\text{asymptotic}} \\
    \mathcal{L}_{\text{custom}} &= \frac{\chi^2}{\text{dof}} + \epsilon \sum_{b_T \in \{b_{T}^{\text{fixed}}\}} \sum_{b_L \in \{b_{L}^{\text{large}}\}} \left| \tAmp_{2B}^{\text{Re, model}}(b_L, b_T) \right|
\end{align}

By evaluating fitness using $\mathcal{L}_{\text{custom}}$, \texttt{PySR} is effectively restricted to searching only the subspace of mathematical expressions that simultaneously fit the finite lattice data and rigorously obey the asymptotic decay constraints required by QCD phenomenology. The explicit implementation of this objective function in the Julia language, as passed to the \texttt{PySR} engine, is shown below. The asymptotic penalty evaluates each candidate expression at five test coordinates in the regime $b_L \gg 1$, spanning staple extents $\eta \in \{6, 8, 10\}$ and longitudinal separations up to $b_L = 80a$. The regularization weight is set to $\epsilon = 0.1$.

\input{PySR_outputs/pysr_custom_loss_code}

\subsubsection{Pareto Front of Discovered Models}

The execution of this physics-informed symbolic regression yields a ``Pareto Front'' of candidate models: a ranked list of equations that offer the optimal trade-offs between mathematical simplicity (measured by expression tree complexity $C$) and predictive accuracy (measured by the custom loss $\mathcal{L}_{\text{custom}}$). The full Pareto front for the real unpolarized amplitude $\tAmp_{2B}^{\text{Re}}$ is presented in Table~\ref{tab:pysr_pareto_front}.

\input{PySR_outputs/pysr_equations_table}

The Pareto front reveals a physically illuminating hierarchy of increasingly sophisticated analytical structures. At the lowest complexities ($C \leq 4$), \texttt{PySR} captures only the overall $\eta$-dependence of the amplitude, discovering a pure exponential decay $\sim (0.499)^\eta \approx e^{-0.695\eta}$, which is broadly consistent with an area-law suppression of the extended Wilson line. At intermediate complexities ($C = 7$--$12$), the algorithm introduces explicit spatial $b$-dependence through Lorentzian-type power-law denominators of the form $(1 + b_L^2 + b_T^2)^{-j}$, coupled with various crude $\eta$-models (linear, power-law). Crucially, at no point along the Pareto front does the algorithm discover a Gaussian spatial profile $e^{-cb^2}$; instead, it consistently converges on the heavier power-law tail structure, independently corroborating the pathological failure of the Gaussian ansatz established in Section~\ref{sec:gaussian_transformation}.

The physically optimal models emerge at complexities $C = 14$ and $C = 16$. The $C = 14$ expression achieves the cleanest factorized form:
\begin{equation}
    \tAmp_{2B}^{\text{Re}} = \frac{39.71}{(1 + b_L^2 + b_T^2)^{1.39}} \, e^{-0.534\eta} \ ,
    \label{eq:pysr_C14}
\end{equation}
which is a product of an exponential decay in the staple extent $\eta$ and a generalized Lorentzian power-law decay in the total squared separation $b^2 = b_L^2 + b_T^2$. The $C = 16$ model introduces a small additive constant offset of $-0.00127$, reducing the loss to $\mathcal{L}_{\text{custom}} = 2.03$:
\begin{equation}
    \tAmp_{2B}^{\text{Re}} = \frac{245.5}{(1 + b_L^2 + b_T^2)^{1.26}} \, e^{-0.468\eta} - 0.00127 \ .
    \label{eq:pysr_C16}
\end{equation}
Beyond $C = 16$, additional complexity yields only marginal improvements in loss while sacrificing interpretability, confirming that the essential physics is fully captured by the factorized exponential--power-law structure.

This discovery has a profound implication for the Fourier integrability constraint discussed in Section~\ref{sec:gaussian_transformation}. The power-law tail $(1 + b^2)^{-j}$ with $j > 1/2$ decays algebraically at large separations, which is slower than a Gaussian but remains absolutely integrable over the real line. Unlike the Gaussian model, where the sign of $(\alpha - \beta)$ could render the Fourier integral divergent, the power-law denominator guarantees convergent integrals for all physical values of the exponent $j$. Furthermore, the factorized structure $e^{-f\eta} \times g(b^2)$ is precisely the form required for the shared $\eta$-dependent exponential to cancel analytically in the Sivers shift ratio, as demonstrated in Section~\ref{sec:simultaneous_fit_strategy}.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=1\linewidth]{PySR_outputs/01_A2B_PySR_Benchmark_Fits.pdf}
    \caption{Multi-panel comparison of the top \texttt{PySR}-discovered analytical models ($C = 11, 14, 16, 17$) against the lattice data for $\tAmp_{2B}^{\text{Re}}$ at fixed staple extents $\eta = 6, 7, 8, 10$ (with $P_1 = -1$ and $|\vect{b}| \geq 3a$). Each panel displays the amplitude as a function of the longitudinal separation $b_L/a$ at fixed transverse separations $b_T$, with the four Pareto-front models overlaid. The two exponential-$\eta$ models ($C = 14$, solid; $C = 16$, dashed) provide the most balanced description across all $\eta$ values, while the pure power-law-$\eta$ models ($C = 11$, dotted; $C = 17$, dot-dashed) exhibit slight systematic deviations at the extremes of the $\eta$ range.}
    \label{fig:pysr_benchmark_fits}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.95\linewidth]{PySR_outputs/02_Gaussian_StraightLine_Test.pdf}
    \caption{Diagnostic straight-line test for the Gaussian hypothesis. If the amplitude followed a pure Gaussian profile $\tAmp_{2B}^{\text{Re}} \propto e^{-c\,b^2}$, the data would appear as a straight line on a logarithmic ordinate versus $b^2$ (red dashed line, optimal Gaussian fit with $c = 0.058$). The lattice data (black markers) exhibit clear upward curvature away from the Gaussian baseline at large $b^2$, indicating a heavier-than-Gaussian tail that is accurately captured by the \texttt{PySR}-discovered power-law form $(1 + b^2)^{-1.39}$ (blue solid line). This provides direct, model-independent evidence that the spatial decay of the non-local quark bilinear is governed by an algebraic power law rather than a Gaussian exponential.}
    \label{fig:gaussian_straightline_test}
\end{figure}

The visual comparison in Figure~\ref{fig:pysr_benchmark_fits} confirms the quantitative superiority of the \texttt{PySR}-discovered models. Across all staple extents, the factorized $C = 14$ and $C = 16$ expressions track the lattice data throughout the accessible $(b_L, b_T)$ grid, while the pure power-law-$\eta$ models ($C = 11, 17$) exhibit slight systematic tension at the boundary $\eta$ values where their $\eta^{-n}$ dependence departs from the smoother exponential decay.

The straight-line diagnostic in Figure~\ref{fig:gaussian_straightline_test} provides definitive, model-independent evidence for the power-law spatial structure. Under the Gaussian hypothesis, plotting $\tAmp_{2B}^{\text{Re}}$ on a logarithmic ordinate against $b^2$ should yield a straight line with slope $-c$. The clear upward curvature observed in the lattice data at large $b^2$ unambiguously rules out the Gaussian form: the true amplitude decays more slowly than any exponential, precisely as expected for a generalized Lorentzian power-law tail. This diagnostic independently validates the functional form discovered by the genetic algorithm, confirming that the choice of $(1 + b^2)^{-j}$ is not an artifact of the evolutionary search but a genuine feature of the underlying non-perturbative physics.

These results establish the \texttt{PySR}-discovered Lorentzian power-law structure as the physically correct spatial profile for $\tAmp_{2B}^{\text{Re}}$. In the following sections, we incorporate this functional form into the full $\eta$-dependent parameterization and extend it to the remaining amplitudes for the global simultaneous fit.


\subsection{Discretization Effects and Short-Distance Data Exclusion}
\label{sec:discretization_effects}

Before deploying the physics-informed symbolic regression (or any subsequent global fitting procedure) on the extracted lattice amplitudes, we must carefully address the validity limits of our discretized spacetime. Lattice QCD acts as an ultraviolet (UV) regulator by defining the theory on a discrete grid with a finite lattice spacing $a$. Consequently, physical observables evaluated at length scales comparable to $a$ are heavily contaminated by discretization artifacts.

For our specific evaluation of non-local Transverse Momentum Dependent (TMD) observables, this discrete geometry poses a significant challenge at short quark-separation distances. When the magnitude of the spatial separation vector $\vect{b}$ is very small (e.g., $|\vect{b}| \sim 1a$ or $2a$), the underlying continuous rotational symmetry of QCD is severely broken down to the discrete hypercubic group of the lattice. Furthermore, the step-wise Wilson lines constructed to connect these closely spaced fields suffer from severe $\mathcal{O}(a)$ and $\mathcal{O}(a^2)$ finite-spacing errors. Fitting phenomenological continuum models to these short-distance lattice points would force the algorithm to artificially parameterize unphysical lattice UV noise rather than the true non-perturbative structure of the nucleon.

To rigorously insulate our continuous invariant parameterizations from short-distance discretization effects, we impose a strict spatial cut on our lattice dataset before any model fitting. Specifically, we exclude all data points where the magnitude of the spatial separation is less than three lattice units:
\begin{equation}
    |\vect{b}| = \sqrt{b_L^2 + b_T^2} \ge 3a \ .
\end{equation}

By isolating the dataset to $b \ge 3a$, we guarantee that the non-local operator spans a sufficient number of lattice sites such that the step-wise gauge link approaches a relatively smooth, continuous trajectory. This spatial restriction ensures that the physics-informed \texttt{PySR} algorithm and the resulting Fourier transforms over $P \cdot b$ are trained exclusively on the long-range, non-perturbative dynamics driving the Sivers effect, safely isolated from the corrupting influence of the discrete ultraviolet regulator.

\begin{figure}[H]
    \centering
    \includegraphics[width=1\linewidth]{plots/short_distance/A2B_Re_b_lessthan3_and_ge3.pdf}
    \caption{Plots of $\tAmp^{Re}_{2B}$ amplitude showing data $|\vect{b}| < 3a$ curving in opposite way that data at $|\vect{b}| \ge 3a$ for fixed momentum ($P=-1$) and fixed $\eta$ (say $\eta=8$)}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=1\linewidth]{plots/short_distance/A12B_Re_b_lessthan3_and_ge3.pdf}
    \caption{Plots of $\tAmp^{Re}_{12B}$ amplitude showing data $|\vect{b}| < 3a$ curving in opposite way that data at $|\vect{b}| \ge 3a$ for fixed momentum ($P=-1$) and fixed $\eta$ (say $\eta=8$)}
\end{figure}

All subsequent stability analyses, global fits, and extracted phenomenological parameters discussed in this chapter are derived strictly from this artifact-suppressed ($|\vect{b}| \ge 3a$) dataset.


\section{$\tAmp^{Re}_{2B}$: $\eta$-dependent Gaussian-type fit : $b\geq3$}
\subsection{At fixed $\eta$}
On fitting a Gaussian function at any given $\eta$
\begin{align}
    \tAmp^{Re}_{2B}=ae^{-cb_L^2}e^{-db_T^2}
\end{align}
we compared $\chi^2/DoF$. The differences in the parameters $c$ and $d$ will provide information about stability in $P.b$ space. 
\begin{figure}[h!]
    \centering
    \includegraphics[width=1\linewidth]{Gaussian_type_fit_Real_A2B/Gaussian_eta_p-1.eps}
    \caption{Gaussian fit for each $\eta$, $b\geq3$ and $P_L=-1$}
    \label{fig:placeholder}
\end{figure}
\begin{figure}[h!]
    \centering
    \includegraphics[width=1\linewidth]{Gaussian_type_fit_Real_A2B/Gaussian_eta_p-2.eps}
    \caption{Gaussian fit for each $\eta$, $b\geq3$ and $P_L=-2$}
    \label{fig:placeholder}
\end{figure}
\begin{figure}[h!]
    \centering
    \includegraphics[width=1\linewidth]{Gaussian_type_fit_Real_A2B/Gaussian_eta_p-3.eps}
    \caption{Gaussian fit for each $\eta$, $b\geq3$ and $P_L=-3$}
    \label{fig:placeholder}
\end{figure}
\begin{figure}[h!]
    \centering
    \includegraphics[width=1\linewidth]{Gaussian_type_fit_Real_A2B/Gaussian_eta_p-4.eps}
    \caption{Gaussian fit for each $\eta$, $b\geq3$ and $P_L=-4$}
    \label{fig:placeholder}
\end{figure}

We also tried a Lorentzian fit for each $\eta$:
\begin{figure}[h!]
    \centering
    \includegraphics[width=1\linewidth]{Gaussian_type_fit_Real_A2B/Lorentzian_eta_p-1.eps}
    \caption{Lorentzian fit for each $\eta$, $b\geq3$ and $P_L=-4$}
    \label{fig:placeholder}
\end{figure}

\pagebreak

~ 
\pagebreak

~ 
\pagebreak

\subsubsection{Finding best Fit: $b\geq3$}
For $P_L=-1$, the range $7\leq\eta\leq10$ is found to be the most stable region for our analysis. We begin the fitting procedure with a simple Gaussian ansatz in $(b_L, b_T)$, which is a common baseline in lattice phenomenology. However, this model yields a high $\chi^2/\text{DoF} \approx 3.54$, indicating that a pure Gaussian decay is too steep to fit the data well.

To address this, we systematically explore several functional variants. We find that introducing a power-law dependence significantly improves the quality of the fit. 

\begin{figure}[h!]
    \centering
    \includegraphics[width=1\linewidth]{Gaussian_type_fit_Real_A2B/FitList_eta78910_p-1.eps}
    \caption{Summary of various functional fits for $\tAmp^{Re}_{2B}$ for $7\leq\eta\leq10$, $b\geq3$ and $P_L=-1$}
    \label{fig:placeholder}
\end{figure}
For $\tAmp^{Re}_{2B}$ for $7\leq\eta\leq10$, $b\geq3$ and $P_L=-1$, the picked model is
{\color{blue}
\begin{align}
    \Aboxed{\tAmp^{Re}_{2B}(b_L, b_T,\eta)&=\frac{ae^{-f\eta}}{(1~+~c\eta^{g}b_{L}^2~+~db_{T}^{2})^{j}}}\\
    \tAmp^{Re}_{2B}(b_L, b_T,\eta)&=\frac{[5.2(12)]e^{-[0.500(15)]\eta}}{(1~+~[0.034(17)]\eta^{[0.50(15)]}b_{L}^2~+~[0.103(29)]b_{T}^{2})^{[2.16(21)]}}
\end{align}
}

\pagebreak
To test the stability and range of $\eta$ and $b_{min}$, we fit:
\begin{itemize}
    \item \begin{figure}[h!]
    \centering
    \includegraphics[width=0.9\linewidth]{Gaussian_type_fit_Real_A2B/Lorentzian_eta7-10_p-1_bleq3.eps}
    \caption{Lorentzian-type Power-Law fit for $\tAmp^{Re}_{2B}$ for $7\leq\eta\leq10$, $b\geq3$ and $P_L=-1$}
    \label{fig:placeholder}
     \end{figure}

     \item \begin{figure}[h!]
    \centering
    \includegraphics[width=0.9\linewidth]{Gaussian_type_fit_Real_A2B/Lorentzian_eta6-10_p-1_bleq3.eps}
    \caption{Lorentzian-type Power-Law fit for $\tAmp^{Re}_{2B}$ for $6\leq\eta\leq10$, $b\geq3$ and $P_L=-1$}
    \label{fig:placeholder}
     \end{figure}

     \item \begin{figure}[h!]
    \centering
    \includegraphics[width=0.9\linewidth]{Gaussian_type_fit_Real_A2B/Lorentzian_eta7-10_p-1_bleq2.eps}
    \caption{Lorentzian-type Power-Law fit for $\tAmp^{Re}_{2B}$ for $7\leq\eta\leq10$, $b\geq2$ and $P_L=-1$}
    \label{fig:placeholder}
     \end{figure}

     \item \begin{figure}[h!]
    \centering
    \includegraphics[width=0.9\linewidth]{Gaussian_type_fit_Real_A2B/Lorentzian_eta6-10_p-1_bleq2.eps}
    \caption{Lorentzian-type Power-Law fit for $\tAmp^{Re}_{2B}$ for $6\leq\eta\leq10$, $b\geq2$ and $P_L=-1$}
    \label{fig:placeholder}
     \end{figure}
\end{itemize}

\pagebreak
\subsubsection{Let's fix the Power Law: $\frac{ae^{-f\eta}}{(1~+~c\eta^{g}b_{L}^2~+~db_{T}^{2})^{2}}$}
\begin{figure}[h!]
    \centering
    \includegraphics[width=1\linewidth]{Gaussian_type_fit_Real_A2B/LorentzianPower2_eta6-10_p-1_bleq3.eps}
\end{figure}

\begin{figure}[h!]
    \centering
    \includegraphics[width=1\linewidth]{Gaussian_type_fit_Real_A2B/LorentzianPower2_eta6-10_p-2_bleq3.eps}
\end{figure}

\begin{figure}[h!]
    \centering
    \includegraphics[width=1\linewidth]{Gaussian_type_fit_Real_A2B/LorentzianPower2_eta5-10_p-3_bleq3.eps}
\end{figure}

\begin{figure}[h!]
    \centering
    \includegraphics[width=1\linewidth]{Gaussian_type_fit_Real_A2B/LorentzianPower2_eta5-9_p-4_bleq3.eps}
\end{figure}


\pagebreak
\subsection{Re A2B: Asymptotic in $\eta$ functions}
\begin{figure}[h!]
    \centering
    \includegraphics[width=1\linewidth]{PowerLaw_etaAsymptotic/A2BfitFT_eta_Asymptotic_P1.pdf}
\end{figure}

\pagebreak
\subsection{Asymptotic in $\eta$ functions (Wider Fourier transform ansatzs)}
\begin{figure}[h!]
    \centering
    \includegraphics[width=1\linewidth]{PowerLaw_etaAsymptotic/A2Bfit_widerFT_eta_Asymptotic_P1.pdf}
\end{figure}

\begin{figure}[h!]
    \centering
    \includegraphics[width=1\linewidth]{PowerLaw_etaAsymptotic/A2Bfit_widerFT_eta_Asymptotic_P2.pdf}
\end{figure}

\begin{figure}[h!]
    \centering
    \includegraphics[width=1\linewidth]{PowerLaw_etaAsymptotic/A2Bfit_widerFT_eta_Asymptotic_P3.pdf}
\end{figure}

\begin{figure}[h!]
    \centering
    \includegraphics[width=1\linewidth]{PowerLaw_etaAsymptotic/A2Bfit_widerFT_eta_Asymptotic_compareP123.pdf}
\end{figure}


\pagebreak
~

\pagebreak
~

\pagebreak
~

\pagebreak
~

\section{Candidate Selection and Simultaneous Fitting Strategy}
\label{sec:simultaneous_fit_strategy}

Through a rigorous $\chi^2/\text{dof}$ minimizations and sequential fitting procedures, we systematically narrowed down the potential candidate models for the phenomenologically relevant invariant amplitudes: $\tAmp_{2B}^{\text{Re}}$, $\tAmp_{2B}^{\text{Im}}$, and $\tAmp_{12B}^{\text{Re}}$. As previously noted, $\tAmp_{12B}^{\text{Im}}$ is excluded from this modeling as the lattice data remains statistically compatible with zero. 

A crucial finding from these sequential fits is that all three surviving amplitudes exhibit a universal $\eta$-dependence characterized by a Lorentzian-power-law profile damped by a shared exponential decay factor, $e^{-f\eta}$. Because the Sivers shift is defined as a ratio of transverse momentum moments, this shared exponential behavior will be canceled out for the physical momentum fraction $x$ calculation. 

We begin with the definition of the Sivers shift as the ratio of the first transverse momentum moment of the Sivers function to the unpolarized TMD distribution:
\begin{equation}
   \langle \vect{k}_y \rangle_{TU}(\vprp{\bvec}^2,x,\zetahat,\eta v \tcdot P) 
	\ \equiv\ \mN \frac{\tilde f_{1T}^{\perp(1)}(\vprp{\bvec}^2;\zetahat,\ldots,\eta v \tcdot P)}{\tilde f_1^{(0)}(\vprp{\bvec}^2;\zetahat,\ldots,\eta v \tcdot P)} \ .
\end{equation}

By expanding these Fourier-transformed distributions in terms of the invariant effective amplitudes $\tAmp_{12B}$ and $\tAmp_{2B}$, we rewrite the ratio explicitly as an integration over the longitudinal spatial separation $b \cdot P$:
\begin{equation}
    \langle \vect{k}_y \rangle_{TU} = -m_{N} \frac{\int d(b\cdot P)e^{ix(b\cdot P)} \tAmp_{12B}(\bvec^2,\bvec \tcdot P,(\bvec \tcdot P) R(\zetahat^2)/\mN^2,-1/(\mN\zetahat)^2,\eta v \tcdot P)}{\int d(b\cdot P)e^{ix(b\cdot P)}\tAmp_{2B}(\bvec^2,\bvec \tcdot P,(\bvec \tcdot P) R(\zetahat^2)/\mN^2,-1/(\mN\zetahat)^2,\eta v \tcdot P)} \ .
\end{equation}

Next, we decompose the complex effective amplitudes into their real (T-even) and imaginary (T-odd) constituents. Because the lattice data for the imaginary Sivers amplitude is entirely dominated by noise and statistically indistinguishable from zero, we explicitly eliminate $\tAmp_{12B}^{\text{Im}}$ from the numerator:
\begin{equation}
    \langle \vect{k}_y \rangle_{TU} = -m_{N} \frac{\int d(b\cdot P)e^{ix(b\cdot P)} \left[\tAmp_{12B}^{\text{Re}}(\cdots)+i\cancelto{0}{\tAmp_{12B}^{\text{Im}}(\cdots)}\right]}{\int d(b\cdot P)e^{ix(b\cdot P)}\left[\tAmp_{2B}^{\text{Re}}(\cdots)+i\tAmp_{2B}^{\text{Im}}(\cdots)\right]} \ .
\end{equation}

Applying Euler's formula to expand the Fourier kernel $e^{ix(b\cdot P)}$ into its symmetric (cosine) and antisymmetric (sine) trigonometric components allows us to filter the integrals by parity. Because the integration domain is symmetric, any integrand that is overall odd in $b \cdot P$ integrates exactly to zero. Retaining only the non-vanishing even-parity combinations yields:
\begin{equation}
    \langle \vect{k}_y \rangle_{TU} = -m_{N} \frac{\int d(b\cdot P) \cos{\left(x(b\cdot P)\right)}\tAmp_{12B}^{\text{Re}}(\cdots)}{\int d(b\cdot P)\left[\cos{\left(x(b\cdot P)\right)}\tAmp_{2B}^{\text{Re}}(\cdots)-\sin{\left(x(b\cdot P)\right)}\tAmp_{2B}^{\text{Im}}(\cdots)\right]} \ .
\end{equation}
At this stage, we substitute the precise analytical models (in $(b_L,b_T)$ space) discovered during our sequential parameter optimization.
\begin{align}
    \tAmp_{2B}^{\text{Re}}&=\left[\frac{a e^{-f \eta}}{\left[1 + \left(d + c\left(1 - \frac{k_1}{\eta} - \frac{k_2}{\eta^2}\right)\right) b_L^2 + d b_T^2\right]^j}\right]\\
    \tAmp_{12B}^{\text{Re}}&=\left[\frac{-a^{\prime} e^{-f \eta}}{\left[1 + (c^{\prime} + d^{\prime}) b_L^2 + d^{\prime} b_T^2\right]^{j^{\prime}}}\right]\\
    \tAmp_{2B}^{\text{Im}}&=\left[\frac{a^{\prime\prime} b_L e^{-f \eta}}{\left[1 + (c^{\prime\prime} + d^{\prime\prime}) b_L^2 + d^{\prime\prime} b_T^2\right]^{j^{\prime\prime}}}\right]
\end{align}
\textcolor{red}{$\eta$-dependancy for A12B and ImA2B, show plots c vs $1/\eta$}
\textcolor{red}{$\eta$-dependancy $e^{-f\eta}$: Wilson loop: Area'ish law, exponential decay, show Mathematica gave common f}


Substituting these functions explicitly reveals the presence of the shared asymptotic parameter $e^{-f \eta}$:
\begin{equation}
    \langle \vect{k}_y \rangle_{TU} = \frac{-m_{N} \int d(b\cdot P) \cos{\left(x(b\cdot P)\right)}\left[\frac{-a^{\prime} e^{-f \eta}}{\left[1 + (c^{\prime} + d^{\prime}) b_L^2 + d^{\prime} b_T^2\right]^{j^{\prime}}}\right]}{\int d(b\cdot P)\left[\cos{\left(x(b\cdot P)\right)}\left[\frac{a e^{-f \eta}}{\left[1 + \left(d + c\left(1 - \frac{k_1}{\eta} - \frac{k_2}{\eta^2}\right)\right) b_L^2 + d b_T^2\right]^j}\right]-\sin{\left(x(b\cdot P)\right)}\left[\frac{a^{\prime\prime} b_L e^{-f \eta}}{\left[1 + (c^{\prime\prime} + d^{\prime\prime}) b_L^2 + d^{\prime\prime} b_T^2\right]^{j^{\prime\prime}}}\right]\right]} \ .
\end{equation}
Because the $e^{-f\eta}$ term is independent of the integration variable $b \cdot P$, it can be factored out of both the numerator and denominator integrals. This results in the exact analytical cancellation of the shared exponential decay parameter:
\begin{equation}
    \langle \vect{k}_y \rangle_{TU} = \frac{\cancel{e^{-f \eta}} m_{N} \int d(b\cdot P) \cos{\left(x(b\cdot P)\right)}\left[\frac{a^{\prime}}{\left[1 + (c^{\prime} + d^{\prime}) b_L^2 + d^{\prime} b_T^2\right]^{j^{\prime}}}\right]}{\cancel{e^{-f \eta}}\int d(b\cdot P)\left[\cos{\left(x(b\cdot P)\right)}\left[\frac{a}{\left[1 + \left(d + c\left(1 - \frac{k_1}{\eta} - \frac{k_2}{\eta^2}\right)\right) b_L^2 + d b_T^2\right]^j}\right]-\sin{\left(x(b\cdot P)\right)}\left[\frac{a^{\prime\prime} b_L }{\left[1 + (c^{\prime\prime} + d^{\prime\prime}) b_L^2 + d^{\prime\prime} b_T^2\right]^{j^{\prime\prime}}}\right]\right]} \ .
\end{equation}
Finally, we apply the Lorentz-invariant projection derived in Section \ref{sec:lorentz_mapping}, where the longitudinal spatial coordinate is given by $b_L = \frac{(\textcolor{blue}{P \cdot b})}{(-P_L)}$ and $b_L^2+b_T^2=b^2$. Applying these substitutions completely maps the amplitude into the required invariant frame:
\begin{equation}
    \langle \vect{k}_y \rangle_{TU} = \frac{ m_{N} \int d(\textcolor{blue}{b\cdot P}) \left[\frac{a^{\prime}\cos{\left(x(\textcolor{blue}{b\cdot P})\right)}}{\left[1 + (c^{\prime})\frac{(\textcolor{blue}{P \cdot b})^2}{(P_L)^2} + d^{\prime} b^2\right]^{j^{\prime}}}\right]}{\int d(\textcolor{blue}{b\cdot P})\left[\left[\frac{a \cos{\left(x(\textcolor{blue}{b\cdot P})\right)}}{\left[1 + \left(c\left(1 - \frac{k_1}{\eta} - \frac{k_2}{\eta^2}\right)\right) \frac{(\textcolor{blue}{P \cdot b})^2}{(P_L)^2} + d b^2\right]^j}\right]-\left[\frac{a^{\prime\prime} \sin{\left(x(\textcolor{blue}{b\cdot P})\right)} \frac{(\textcolor{blue}{P \cdot b})}{(-P_L)} }{\left[1 + (c^{\prime\prime}) \frac{(\textcolor{blue}{P \cdot b})^2}{(P_L)^2} + d^{\prime\prime} b^2\right]^{j^{\prime\prime}}}\right]\right]} \ .
\end{equation}
To rigorously enforce this shared $\eta$-dependence and capture the physics governing these amplitudes, we must transition from sequential fitting to a comprehensive simultaneous global fit. By forcing the parameter $f$ to be identical across all three amplitude models ($\tAmp_{2B}^{\text{Re}}$, $\tAmp_{2B}^{\text{Im}}$, and $\tAmp_{12B}^{\text{Re}}$), the simultaneous fit guarantees theoretical self-consistency and significantly stabilizes the parameter extraction, correctly reflecting the physical cancellation mechanism shown above.

\subsection{Simultaneous Fit with Full Covariance Matrix}

Evaluating the statistical uncertainties of these highly non-linear, simultaneous parameterizations requires moving beyond a simple uncorrelated $\chi^2$ minimization. Because the amplitudes are extracted from the same underlying gauge field configurations and evaluated across nearby spatial coordinates ($b_L, b_T$), their statistical fluctuations are highly correlated. A traditional uncorrelated fit would improperly weight these data points, leading to artificially deflated uncertainties and potentially biased central values.

To resolve this, we implement a correlated $\chi^2$ minimization utilizing the full covariance matrix. This method mathematically accounts for the substantial cross-correlations inherent in the lattice data, both across different spatial separations and across the real and imaginary amplitude components. 

To robustly estimate these correlations, we employ the Jackknife resampling method. Let $y_i^{(k)}$ represent the lattice evaluation of the $i$-th data point (which spans all spatial coordinates across $\tAmp_{2B}^{\text{Re}}$, $\tAmp_{2B}^{\text{Im}}$, and $\tAmp_{12B}^{\text{Re}}$) in the $k$-th Jackknife bin. The full Jackknife covariance matrix $C_{ij}$ is defined as \cite{Boram2011Covariance}:
\begin{equation}
    C_{ij} = \frac{N-1}{N} \sum_{k=1}^{N} \left( y_i^{(k)} - \bar{y}_i \right) \left( y_j^{(k)} - \bar{y}_j \right) \ ,
\end{equation}
where $N$ is the total number of Jackknife samples and $\bar{y}_i = \frac{1}{N}\sum_k y_i^{(k)}$ is the sample mean. 

Incorporating the inverse covariance matrix correctly weights the orthogonal data directions in the parameter space, heavily penalizing non-physical fluctuations. The global simultaneous fit is then driven strictly by minimizing the correlated $\chi^2$ objective function with respect to the model parameters $\mathbf{p}$:
\begin{equation}
    \chi^2(\mathbf{p}) = \sum_{i,j} \left( \bar{y}_i - F_i(\mathbf{p}) \right) C_{ij}^{-1} \left( \bar{y}_j - F_j(\mathbf{p}) \right) \ ,
\end{equation}
where $F_i(\mathbf{p})$ represents the exact analytical models (derived via \texttt{PySR}) evaluated at the corresponding kinematics. 

To execute this numerically, we utilize the \texttt{lsqfit} \cite{Lepage:2001ym} non-linear least squares fitting package in conjunction with the \texttt{gvar} library for Gaussian error propagation. The \texttt{gvar} library seamlessly binds our data array to the dense Jackknife covariance matrix. The core pipeline for this implementation is detailed in the Python snippet below:

\begin{lstlisting}[language=Python, basicstyle=\ttfamily\footnotesize, frame=single, breaklines=true, numbers=left, caption={Python implementation of the correlated simultaneous fit using the full Jackknife covariance matrix.}, label={lst:cov_fit}]
import numpy as np
import gvar as gv
import lsqfit

# 1. Combine Jackknife samples across all amplitude types
#    Shape: (Total_Data_Points, N_samples)
combined_jk = np.vstack([jk_re, jk_im, jk_reA12B])
N_samples = combined_jk.shape[1]

# 2. Calculate the Full Jackknife Covariance Matrix
mean_data = np.mean(combined_jk, axis=1)
diff = combined_jk - mean_data[:, None]
cov_matrix = ((N_samples - 1) / N_samples) * (diff @ diff.T)

# 3. Perform the Non-Linear Least Squares Fit over Jackknife bins
for i in range(N_samples):
    y_sample_i = combined_jk[:, i]
    
    # Bind the current sample to the full covariance matrix
    y_gvar_i = gv.gvar(y_sample_i, cov_matrix)
    
    # Split the bound variables back into their respective amplitude dictionaries
    y_dict = {
        'Re': y_gvar_i[:N_re],
        'Im': y_gvar_i[N_re:(N_re + N_im)],
        'ReA12B': y_gvar_i[(N_re + N_im):]
    }
    
    # Execute the correlated chi-square minimization with Bayesian priors
    fit = lsqfit.nonlinear_fit(data=(x_dict, y_dict), 
                               prior=current_prior, 
                               fcn=fcn)
                               
    # Track the fitted parameters for subsequent analysis
    # ...
\end{lstlisting}

This approach ensures that the global simultaneous fit is driven strictly by the underlying physics, yielding final extracted parameters and propagated error bands that are statistically rigorous and mathematically robust. The ensuing subsections detail the results of these full simultaneous covariance matrix fits evaluated at discrete lattice momenta.

\pagebreak
\renewcommand{\PLval}{-1}
\subsubsection{\texorpdfstring{Simultaneous Fit Results for $\vect{P}\cdot a\hat{L}/(2\pi)=\{\PLval,0,0\}$}{TEXT}}

The extracted parameters from the global simultaneous fit for the lowest simulated nucleon momentum, $P_1 = -1$ ($\hat{\zeta} \approx 0.091$), are summarized in the table below. By mathematically enforcing the shared asymptotic decay parameter $f$ across all three effective amplitudes ($\tAmp_{2B}^{\text{Re}}$, $\tAmp_{2B}^{\text{Im}}$, and $\tAmp_{12B}^{\text{Re}}$), the model successfully stabilizes the extraction of the multi-dimensional spatial profiles while preserving the physical cancellation required for the Sivers shift ratio.

\input{SimultaneousFitsCov/Fit_Table_PL\PLval_bmin3}

Figures \ref{fig:A2B_amplitudes_fit_P1} and \ref{fig:combined_fit_P1} visually demonstrate the quality of this correlated minimization. The simultaneous parameterization (represented by the continuous surfaces and shaded error bands) exhibits excellent agreement with the discrete lattice data across both the native Cartesian $(b_L/a, b_T/a)$ grid and the transformed Lorentz-invariant $(P \cdot b, b^2)$ phase space. Furthermore, the explicit exclusion of the short-distance data ($|\vect{b}| < 3a$) ensures the fit is not artificially skewed by ultraviolet lattice artifacts, allowing the global model to cleanly capture the true non-perturbative long-range dynamics.

\begin{figure}[htbp]
    \centering
    \begin{tabular}{ccc}
        % Column Headers
        \textbf{$\widetilde{A}^{Re}_{2B}$} & \textbf{$\widetilde{A}^{Im}_{2B}$} & \textbf{$\widetilde{A}^{Re}_{12B}$} \\[2ex]
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA12B_Pb_bsq_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ImA2B_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA12B_PL\PLval_WithFit_3D.pdf}\\[2ex]
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA2B_Pb_bsq_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ImA2B_Pb_bsq_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA12B_Pb_bsq_PL\PLval_WithFit_3D.pdf} \\
    \end{tabular}
    \caption{Fit for $\widetilde{A}^{Re}_{2B}$, $\widetilde{A}^{Im}_{2B}$ \& $\widetilde{A}^{Re}_{12B}$ amplitudes for $\vect{P}\cdot a\hat{L}/(2\pi)=\{\PLval,0,0\}$. The top row displays the $(b_{L}/a,b_{T}/a,\eta|\vect{v}|/a)-$space, while the bottom row displays the $((\vect{P}\cdot \vect{b})\cdot\hat{L}/(2\pi),b^{2}/a^2,\eta|\vect{v}|/a)-$space with the global simultaneous fit overlaid.}
    \label{fig:A2B_amplitudes_fit_P1}
\end{figure}

\begin{figure}[htbp]
    \centering
    \begin{tabular}{c}
        \includegraphics[width=0.9\linewidth]{plots/Amps_Fit/Combined_eta8_PL\PLval_WithFit_2D.pdf} \\
        \includegraphics[width=0.9\linewidth]{plots/Amps_Fit/Combined_Pb_bsq_eta8_PL\PLval_WithFit_2D.pdf} \\
    \end{tabular}
    \caption{Fit for $\widetilde{A}^{Re}_{2B}$, $\widetilde{A}^{Im}_{2B}$ \& $\widetilde{A}^{Re}_{12B}$ amplitudes for $\vect{P}\cdot a\hat{L}/(2\pi)=\{\PLval,0,0\}$ at a fixed staple extent $\eta|\vect{v}|/a=8$. The top two rows display the $(b_{L}/a,b_{T}/a)-$space, while the bottom two rows display the $((\vect{P}\cdot \vect{b})\cdot\hat{L}/(2\pi),b^{2}/a^2)-$space with the fit.}
    \label{fig:combined_fit_P1}
\end{figure}

\pagebreak

With the continuous parametrization of the effective amplitudes successfully stabilized, we proceed to analytically evaluate the Fourier integrals over the invariant scalar $P \cdot b$. The results of these integrations, which yield the physical momentum fraction $x$-dependent unpolarized distribution and the consequent Sivers shift, are tabulated below.

\input{SimultaneousFitsCov/Integration_Table_PL\PLval_bmin3}

Finally, the necessity and statistical rigor of employing a fully correlated $\chi^2$ minimization are explicitly confirmed by visualizing the Jackknife covariance matrix, shown in the figures below. The dense off-diagonal elements visible in both the 2D correlation matrix and the 3D surface plot indicate strong cross-correlations between the spatial lattice points and across the distinct amplitude components. By mathematically incorporating this exact covariance structure into the fitting pipeline via \texttt{lsqfit}, we appropriately weight the heavily correlated physical fluctuations and guarantee that the propagated error bands on the final extracted Sivers shift are statistically robust.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.5\linewidth]{SimultaneousFitsCov/SimulFitCovMatrix-A12B-A2B-FT-a_im-a_re-a_reA12B-c_im-c_re-c_reA12B-d_im-d_re-d_reA12B-f-j_im-j_re-j_reA12B-k1_re-k2_re__bmin3_eta610_PL\PLval.pdf}
    \caption{2D representation of the full Jackknife covariance matrix utilized in the simultaneous fit for $P_1=-1$. The prominent off-diagonal bands highlight the strong correlations across the parameter space.}
    \label{fig:cov_matrix_2d_P1}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\linewidth]{SimultaneousFitsCov/SimulFitCovMatrix-A12B-A2B-FT-3D_a_im-a_re-a_reA12B-c_im-c_re-c_reA12B-d_im-d_re-d_reA12B-f-j_im-j_re-j_reA12B-k1_re-k2_re__bmin3_eta610_PL\PLval.pdf}
    \caption{3D surface plot of the Jackknife covariance matrix for $P_1=-1$, visually emphasizing the magnitude of the cross-correlations between the real and imaginary amplitude components.}
    \label{fig:cov_matrix_3d_P1}
\end{figure}
\pagebreak

\renewcommand{\PLval}{-2}
\subsubsection{\texorpdfstring{$\vect{P}\cdot a\hat{L}/(2\pi)=\{\PLval,0,0\}$}{TEXT}}
\input{SimultaneousFitsCov/Fit_Table_PL\PLval_bmin3}
\begin{figure}[htbp]
    \centering
    \begin{tabular}{ccc}
        % Column Headers
        \textbf{$\widetilde{A}^{Re}_{2B}$} & \textbf{$\widetilde{A}^{Im}_{2B}$} & \textbf{$\widetilde{A}^{Re}_{12B}$} \\[2ex]
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA12B_Pb_bsq_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ImA2B_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA12B_PL\PLval_WithFit_3D.pdf}\\[2ex]
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA2B_Pb_bsq_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ImA2B_Pb_bsq_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA12B_Pb_bsq_PL\PLval_WithFit_3D.pdf} \\
    \end{tabular}
    \caption{Fit for $\widetilde{A}^{Re}_{2B}$, $\widetilde{A}^{Im}_{2B}$ \& $\widetilde{A}^{Re}_{12B}$ amplitudes for $\vect{P}\cdot a\hat{L}/(2\pi)=\{\PLval,0,0\}$. The top row displays the $(b_{L}/a,b_{T}/a,\eta|\vect{v}|/a)-$space, while the bottom row displays the $((\vect{P}\cdot \vect{b})\cdot\hat{L}/(2\pi),b^{2}/a^2,\eta|\vect{v}|/a)-$space with the fit.}
    \label{fig:A2B_amplitudes_fit_P2}
\end{figure}
\begin{figure}[htbp]
    \centering
    \begin{tabular}{c}
        \includegraphics[width=0.9\linewidth]{plots/Amps_Fit/Combined_eta8_PL\PLval_WithFit_2D.pdf} \\
        \includegraphics[width=0.9\linewidth]{plots/Amps_Fit/Combined_Pb_bsq_eta8_PL\PLval_WithFit_2D.pdf} \\
    \end{tabular}
    \caption{Fit for $\widetilde{A}^{Re}_{2B}$, $\widetilde{A}^{Im}_{2B}$ \& $\widetilde{A}^{Re}_{12B}$ amplitudes for $\vect{P}\cdot a\hat{L}/(2\pi)=\{\PLval,0,0\}$ at fixed $\eta|\vect{v}|/a=8$. The top two rows display the $(b_{L}/a,b_{T}/a)-$space, while the bottom two rows display the $((\vect{P}\cdot \vect{b})\cdot\hat{L}/(2\pi),b^{2}/a^2)-$space with the fit.}
    \label{fig:combined_fit_P2}
\end{figure}
\pagebreak
\input{SimultaneousFitsCov/Integration_Table_PL\PLval_bmin3}
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.5\linewidth]{SimultaneousFitsCov/SimulFitCovMatrix-A12B-A2B-FT-a_im-a_re-a_reA12B-c_im-c_re-c_reA12B-d_im-d_re-d_reA12B-f-j_im-j_re-j_reA12B-k1_re-k2_re__bmin3_eta610_PL\PLval.pdf}
\end{figure}
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\linewidth]{SimultaneousFitsCov/SimulFitCovMatrix-A12B-A2B-FT-3D_a_im-a_re-a_reA12B-c_im-c_re-c_reA12B-d_im-d_re-d_reA12B-f-j_im-j_re-j_reA12B-k1_re-k2_re__bmin3_eta610_PL\PLval.pdf}
\end{figure}

\pagebreak
\renewcommand{\PLval}{-3}
\subsubsection{\texorpdfstring{$\vect{P}\cdot a\hat{L}/(2\pi)=\{\PLval,0,0\}$}{TEXT}}
\input{SimultaneousFitsCov/Fit_Table_PL\PLval_bmin3}
\begin{figure}[htbp]
    \centering
    \begin{tabular}{ccc}
        % Column Headers
        \textbf{$\widetilde{A}^{Re}_{2B}$} & \textbf{$\widetilde{A}^{Im}_{2B}$} & \textbf{$\widetilde{A}^{Re}_{12B}$} \\[2ex]
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA12B_Pb_bsq_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ImA2B_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA12B_PL\PLval_WithFit_3D.pdf}\\[2ex]
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA2B_Pb_bsq_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ImA2B_Pb_bsq_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA12B_Pb_bsq_PL\PLval_WithFit_3D.pdf} \\
    \end{tabular}
    \caption{Fit for $\widetilde{A}^{Re}_{2B}$, $\widetilde{A}^{Im}_{2B}$ \& $\widetilde{A}^{Re}_{12B}$ amplitudes for $\vect{P}\cdot a\hat{L}/(2\pi)=\{\PLval,0,0\}$. The top row displays the $(b_{L}/a,b_{T}/a,\eta|\vect{v}|/a)-$space, while the bottom row displays the $((\vect{P}\cdot \vect{b})\cdot\hat{L}/(2\pi),b^{2}/a^2,\eta|\vect{v}|/a)-$space with the fit.}
    \label{fig:A2B_amplitudes_fit_P3}
\end{figure}
\begin{figure}[htbp]
    \centering
    \begin{tabular}{c}
        \includegraphics[width=0.9\linewidth]{plots/Amps_Fit/Combined_eta8_PL\PLval_WithFit_2D.pdf} \\
        \includegraphics[width=0.9\linewidth]{plots/Amps_Fit/Combined_Pb_bsq_eta8_PL\PLval_WithFit_2D.pdf} \\
    \end{tabular}
    \caption{Fit for $\widetilde{A}^{Re}_{2B}$, $\widetilde{A}^{Im}_{2B}$ \& $\widetilde{A}^{Re}_{12B}$ amplitudes for $\vect{P}\cdot a\hat{L}/(2\pi)=\{\PLval,0,0\}$ at fixed $\eta|\vect{v}|/a=8$. The top two rows display the $(b_{L}/a,b_{T}/a)-$space, while the bottom two rows display the $((\vect{P}\cdot \vect{b})\cdot\hat{L}/(2\pi),b^{2}/a^2)-$space with the fit.}
    \label{fig:combined_fit_P3}
\end{figure}
\pagebreak
\input{SimultaneousFitsCov/Integration_Table_PL\PLval_bmin3}
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.5\linewidth]{SimultaneousFitsCov/SimulFitCovMatrix-A12B-A2B-FT-a_im-a_re-a_reA12B-c_im-c_re-c_reA12B-d_im-d_re-d_reA12B-f-j_im-j_re-j_reA12B-k1_re-k2_re__bmin3_eta610_PL\PLval.pdf}
\end{figure}
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\linewidth]{SimultaneousFitsCov/SimulFitCovMatrix-A12B-A2B-FT-3D_a_im-a_re-a_reA12B-c_im-c_re-c_reA12B-d_im-d_re-d_reA12B-f-j_im-j_re-j_reA12B-k1_re-k2_re__bmin3_eta610_PL\PLval.pdf}
\end{figure}

\pagebreak
\renewcommand{\PLval}{-4}
\subsubsection{\texorpdfstring{$\vect{P}\cdot a\hat{L}/(2\pi)=\{\PLval,0,0\}$}{TEXT}}
\input{SimultaneousFitsCov/Fit_Table_PL\PLval_bmin3}
\begin{figure}[htbp]
    \centering
    \begin{tabular}{ccc}
        % Column Headers
        \textbf{$\widetilde{A}^{Re}_{2B}$} & \textbf{$\widetilde{A}^{Im}_{2B}$} & \textbf{$\widetilde{A}^{Re}_{12B}$} \\[2ex]
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA12B_Pb_bsq_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ImA2B_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA12B_PL\PLval_WithFit_3D.pdf}\\[2ex]
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA2B_Pb_bsq_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ImA2B_Pb_bsq_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA12B_Pb_bsq_PL\PLval_WithFit_3D.pdf} \\
    \end{tabular}
    \caption{Fit for $\widetilde{A}^{Re}_{2B}$, $\widetilde{A}^{Im}_{2B}$ \& $\widetilde{A}^{Re}_{12B}$ amplitudes for $\vect{P}\cdot a\hat{L}/(2\pi)=\{\PLval,0,0\}$. The top row displays the $(b_{L}/a,b_{T}/a,\eta|\vect{v}|/a)-$space, while the bottom row displays the $((\vect{P}\cdot \vect{b})\cdot\hat{L}/(2\pi),b^{2}/a^2,\eta|\vect{v}|/a)-$space with the fit.}
    \label{fig:A2B_amplitudes_fit_P4}
\end{figure}
\begin{figure}[htbp]
    \centering
    \begin{tabular}{c}
        \includegraphics[width=0.9\linewidth]{plots/Amps_Fit/Combined_eta8_PL\PLval_WithFit_2D.pdf} \\
        \includegraphics[width=0.9\linewidth]{plots/Amps_Fit/Combined_Pb_bsq_eta8_PL\PLval_WithFit_2D.pdf} \\
    \end{tabular}
    \caption{Fit for $\widetilde{A}^{Re}_{2B}$, $\widetilde{A}^{Im}_{2B}$ \& $\widetilde{A}^{Re}_{12B}$ amplitudes for $\vect{P}\cdot a\hat{L}/(2\pi)=\{\PLval,0,0\}$ at fixed $\eta|\vect{v}|/a=8$. The top two rows display the $(b_{L}/a,b_{T}/a)-$space, while the bottom two rows display the $((\vect{P}\cdot \vect{b})\cdot\hat{L}/(2\pi),b^{2}/a^2)-$space with the fit.}
    \label{fig:combined_fit_P4}
\end{figure}
\pagebreak
\input{SimultaneousFitsCov/Integration_Table_PL\PLval_bmin3}
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.5\linewidth]{SimultaneousFitsCov/SimulFitCovMatrix-A12B-A2B-FT-a_im-a_re-a_reA12B-c_im-c_re-c_reA12B-d_im-d_re-d_reA12B-f-j_im-j_re-j_reA12B-k1_re-k2_re__bmin3_eta610_PL\PLval.pdf}
\end{figure}
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\linewidth]{SimultaneousFitsCov/SimulFitCovMatrix-A12B-A2B-FT-3D_a_im-a_re-a_reA12B-c_im-c_re-c_reA12B-d_im-d_re-d_reA12B-f-j_im-j_re-j_reA12B-k1_re-k2_re__bmin3_eta610_PL\PLval.pdf}
\end{figure}

\renewcommand{\PLval}{-1}
\subsubsection{\texorpdfstring{$\vect{P}\cdot a\hat{L}/(2\pi)=\{\PLval,0,0\}$}{TEXT}}
\input{SimultaneousFitsCov/Fit_Table_PL\PLval_bmin3}
\begin{figure}[htbp]
    \centering
    \begin{tabular}{ccc}
        % Column Headers
        \textbf{$\widetilde{A}^{Re}_{2B}$} & \textbf{$\widetilde{A}^{Im}_{2B}$} & \textbf{$\widetilde{A}^{Re}_{12B}$} \\[2ex]
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA12B_Pb_bsq_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ImA2B_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA12B_PL\PLval_WithFit_3D.pdf}\\[2ex]
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA2B_Pb_bsq_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ImA2B_Pb_bsq_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA12B_Pb_bsq_PL\PLval_WithFit_3D.pdf} \\
    \end{tabular}
    \caption{Fit for $\widetilde{A}^{Re}_{2B}$, $\widetilde{A}^{Im}_{2B}$ \& $\widetilde{A}^{Re}_{12B}$ amplitudes for $\vect{P}\cdot a\hat{L}/(2\pi)=\{\PLval,0,0\}$. The top row displays the $(b_{L}/a,b_{T}/a,\eta|\vect{v}|/a)-$space, while the bottom row displays the $((\vect{P}\cdot \vect{b})\cdot\hat{L}/(2\pi),b^{2}/a^2,\eta|\vect{v}|/a)-$space with the fit.}
    \label{fig:A2B_amplitudes}
\end{figure}
\begin{figure}[htbp]
    \centering
    \begin{tabular}{c}
        \includegraphics[width=0.9\linewidth]{plots/Amps_Fit/Combined_eta8_PL\PLval_WithFit_2D.pdf} \\
        \includegraphics[width=0.9\linewidth]{plots/Amps_Fit/Combined_Pb_bsq_eta8_PL\PLval_WithFit_2D.pdf} \\
    \end{tabular}
    \caption{Fit for $\widetilde{A}^{Re}_{2B}$, $\widetilde{A}^{Im}_{2B}$ \& $\widetilde{A}^{Re}_{12B}$ amplitudes for $\vect{P}\cdot a\hat{L}/(2\pi)=\{\PLval,0,0\}$ at fixed $\eta|\vect{v}|/a=8$. The top two row displays the $(b_{L}/a,b_{T}/a)-$space, while the bottom two row displays the $((\vect{P}\cdot \vect{b})\cdot\hat{L}/(2\pi),b^{2}/a^2)-$space with the fit.}
    \label{fig:placeholder}
\end{figure}
\pagebreak
\input{SimultaneousFitsCov/Integration_Table_PL\PLval_bmin3}
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.5\linewidth]{SimultaneousFitsCov/SimulFitCovMatrix-A12B-A2B-FT-a_im-a_re-a_reA12B-c_im-c_re-c_reA12B-d_im-d_re-d_reA12B-f-j_im-j_re-j_reA12B-k1_re-k2_re__bmin3_eta610_PL\PLval.pdf}
\end{figure}
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\linewidth]{SimultaneousFitsCov/SimulFitCovMatrix-A12B-A2B-FT-3D_a_im-a_re-a_reA12B-c_im-c_re-c_reA12B-d_im-d_re-d_reA12B-f-j_im-j_re-j_reA12B-k1_re-k2_re__bmin3_eta610_PL\PLval.pdf}
\end{figure}

\pagebreak
\renewcommand{\PLval}{-2}
\subsubsection{\texorpdfstring{$\vect{P}\cdot a\hat{L}/(2\pi)=\{\PLval,0,0\}$}{TEXT}}
\input{SimultaneousFitsCov/Fit_Table_PL\PLval_bmin3}
\begin{figure}[htbp]
    \centering
    \begin{tabular}{ccc}
        % Column Headers
        \textbf{$\widetilde{A}^{Re}_{2B}$} & \textbf{$\widetilde{A}^{Im}_{2B}$} & \textbf{$\widetilde{A}^{Re}_{12B}$} \\[2ex]
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA12B_Pb_bsq_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ImA2B_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA12B_PL\PLval_WithFit_3D.pdf}\\[2ex]
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA2B_Pb_bsq_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ImA2B_Pb_bsq_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA12B_Pb_bsq_PL\PLval_WithFit_3D.pdf} \\
    \end{tabular}
    \caption{Fit for $\widetilde{A}^{Re}_{2B}$, $\widetilde{A}^{Im}_{2B}$ \& $\widetilde{A}^{Re}_{12B}$ amplitudes for $\vect{P}\cdot a\hat{L}/(2\pi)=\{\PLval,0,0\}$. The top row displays the $(b_{L}/a,b_{T}/a,\eta|\vect{v}|/a)-$space, while the bottom row displays the $((\vect{P}\cdot \vect{b})\cdot\hat{L}/(2\pi),b^{2}/a^2,\eta|\vect{v}|/a)-$space with the fit.}
    \label{fig:A2B_amplitudes}
\end{figure}
\begin{figure}[htbp]
    \centering
    \begin{tabular}{c}
        \includegraphics[width=0.9\linewidth]{plots/Amps_Fit/Combined_eta8_PL\PLval_WithFit_2D.pdf} \\
        \includegraphics[width=0.9\linewidth]{plots/Amps_Fit/Combined_Pb_bsq_eta8_PL\PLval_WithFit_2D.pdf} \\
    \end{tabular}
    \caption{Fit for $\widetilde{A}^{Re}_{2B}$, $\widetilde{A}^{Im}_{2B}$ \& $\widetilde{A}^{Re}_{12B}$ amplitudes for $\vect{P}\cdot a\hat{L}/(2\pi)=\{\PLval,0,0\}$ at fixed $\eta|\vect{v}|/a=8$. The top two row displays the $(b_{L}/a,b_{T}/a)-$space, while the bottom two row displays the $((\vect{P}\cdot \vect{b})\cdot\hat{L}/(2\pi),b^{2}/a^2)-$space with the fit.}
    \label{fig:placeholder}
\end{figure}
\pagebreak
\input{SimultaneousFitsCov/Integration_Table_PL\PLval_bmin3}
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.5\linewidth]{SimultaneousFitsCov/SimulFitCovMatrix-A12B-A2B-FT-a_im-a_re-a_reA12B-c_im-c_re-c_reA12B-d_im-d_re-d_reA12B-f-j_im-j_re-j_reA12B-k1_re-k2_re__bmin3_eta610_PL\PLval.pdf}
\end{figure}
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\linewidth]{SimultaneousFitsCov/SimulFitCovMatrix-A12B-A2B-FT-3D_a_im-a_re-a_reA12B-c_im-c_re-c_reA12B-d_im-d_re-d_reA12B-f-j_im-j_re-j_reA12B-k1_re-k2_re__bmin3_eta610_PL\PLval.pdf}
\end{figure}

\pagebreak
\renewcommand{\PLval}{-3}
\subsubsection{\texorpdfstring{$\vect{P}\cdot a\hat{L}/(2\pi)=\{\PLval,0,0\}$}{TEXT}}
\input{SimultaneousFitsCov/Fit_Table_PL\PLval_bmin3}
\begin{figure}[htbp]
    \centering
    \begin{tabular}{ccc}
        % Column Headers
        \textbf{$\widetilde{A}^{Re}_{2B}$} & \textbf{$\widetilde{A}^{Im}_{2B}$} & \textbf{$\widetilde{A}^{Re}_{12B}$} \\[2ex]
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA12B_Pb_bsq_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ImA2B_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA12B_PL\PLval_WithFit_3D.pdf}\\[2ex]
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA2B_Pb_bsq_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ImA2B_Pb_bsq_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA12B_Pb_bsq_PL\PLval_WithFit_3D.pdf} \\
    \end{tabular}
    \caption{Fit for $\widetilde{A}^{Re}_{2B}$, $\widetilde{A}^{Im}_{2B}$ \& $\widetilde{A}^{Re}_{12B}$ amplitudes for $\vect{P}\cdot a\hat{L}/(2\pi)=\{\PLval,0,0\}$. The top row displays the $(b_{L}/a,b_{T}/a,\eta|\vect{v}|/a)-$space, while the bottom row displays the $((\vect{P}\cdot \vect{b})\cdot\hat{L}/(2\pi),b^{2}/a^2,\eta|\vect{v}|/a)-$space with the fit.}
    \label{fig:A2B_amplitudes}
\end{figure}
\begin{figure}[htbp]
    \centering
    \begin{tabular}{c}
        \includegraphics[width=0.9\linewidth]{plots/Amps_Fit/Combined_eta8_PL\PLval_WithFit_2D.pdf} \\
        \includegraphics[width=0.9\linewidth]{plots/Amps_Fit/Combined_Pb_bsq_eta8_PL\PLval_WithFit_2D.pdf} \\
    \end{tabular}
    \caption{Fit for $\widetilde{A}^{Re}_{2B}$, $\widetilde{A}^{Im}_{2B}$ \& $\widetilde{A}^{Re}_{12B}$ amplitudes for $\vect{P}\cdot a\hat{L}/(2\pi)=\{\PLval,0,0\}$ at fixed $\eta|\vect{v}|/a=8$. The top two row displays the $(b_{L}/a,b_{T}/a)-$space, while the bottom two row displays the $((\vect{P}\cdot \vect{b})\cdot\hat{L}/(2\pi),b^{2}/a^2)-$space with the fit.}
    \label{fig:placeholder}
\end{figure}
\pagebreak
\input{SimultaneousFitsCov/Integration_Table_PL\PLval_bmin3}
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.5\linewidth]{SimultaneousFitsCov/SimulFitCovMatrix-A12B-A2B-FT-a_im-a_re-a_reA12B-c_im-c_re-c_reA12B-d_im-d_re-d_reA12B-f-j_im-j_re-j_reA12B-k1_re-k2_re__bmin3_eta610_PL\PLval.pdf}
\end{figure}
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\linewidth]{SimultaneousFitsCov/SimulFitCovMatrix-A12B-A2B-FT-3D_a_im-a_re-a_reA12B-c_im-c_re-c_reA12B-d_im-d_re-d_reA12B-f-j_im-j_re-j_reA12B-k1_re-k2_re__bmin3_eta610_PL\PLval.pdf}
\end{figure}

\pagebreak
\renewcommand{\PLval}{-4}
\subsubsection{\texorpdfstring{$\vect{P}\cdot a\hat{L}/(2\pi)=\{\PLval,0,0\}$}{TEXT}}
\input{SimultaneousFitsCov/Fit_Table_PL\PLval_bmin3}
\begin{figure}[htbp]
    \centering
    \begin{tabular}{ccc}
        % Column Headers
        \textbf{$\widetilde{A}^{Re}_{2B}$} & \textbf{$\widetilde{A}^{Im}_{2B}$} & \textbf{$\widetilde{A}^{Re}_{12B}$} \\[2ex]
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA12B_Pb_bsq_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ImA2B_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA12B_PL\PLval_WithFit_3D.pdf}\\[2ex]
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA2B_Pb_bsq_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ImA2B_Pb_bsq_PL\PLval_WithFit_3D.pdf} &
        \includegraphics[width=0.3\linewidth]{plots/Amps_Fit/ReA12B_Pb_bsq_PL\PLval_WithFit_3D.pdf} \\
    \end{tabular}
    \caption{Fit for $\widetilde{A}^{Re}_{2B}$, $\widetilde{A}^{Im}_{2B}$ \& $\widetilde{A}^{Re}_{12B}$ amplitudes for $\vect{P}\cdot a\hat{L}/(2\pi)=\{\PLval,0,0\}$. The top row displays the $(b_{L}/a,b_{T}/a,\eta|\vect{v}|/a)-$space, while the bottom row displays the $((\vect{P}\cdot \vect{b})\cdot\hat{L}/(2\pi),b^{2}/a^2,\eta|\vect{v}|/a)-$space with the fit.}
    \label{fig:A2B_amplitudes}
\end{figure}
\begin{figure}[htbp]
    \centering
    \begin{tabular}{c}
        \includegraphics[width=0.9\linewidth]{plots/Amps_Fit/Combined_eta8_PL\PLval_WithFit_2D.pdf} \\
        \includegraphics[width=0.9\linewidth]{plots/Amps_Fit/Combined_Pb_bsq_eta8_PL\PLval_WithFit_2D.pdf} \\
    \end{tabular}
    \caption{Fit for $\widetilde{A}^{Re}_{2B}$, $\widetilde{A}^{Im}_{2B}$ \& $\widetilde{A}^{Re}_{12B}$ amplitudes for $\vect{P}\cdot a\hat{L}/(2\pi)=\{\PLval,0,0\}$ at fixed $\eta|\vect{v}|/a=8$. The top two row displays the $(b_{L}/a,b_{T}/a)-$space, while the bottom two row displays the $((\vect{P}\cdot \vect{b})\cdot\hat{L}/(2\pi),b^{2}/a^2)-$space with the fit.}
    \label{fig:placeholder}
\end{figure}
\pagebreak
\input{SimultaneousFitsCov/Integration_Table_PL\PLval_bmin3}
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.5\linewidth]{SimultaneousFitsCov/SimulFitCovMatrix-A12B-A2B-FT-a_im-a_re-a_reA12B-c_im-c_re-c_reA12B-d_im-d_re-d_reA12B-f-j_im-j_re-j_reA12B-k1_re-k2_re__bmin3_eta610_PL\PLval.pdf}
\end{figure}
\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.7\linewidth]{SimultaneousFitsCov/SimulFitCovMatrix-A12B-A2B-FT-3D_a_im-a_re-a_reA12B-c_im-c_re-c_reA12B-d_im-d_re-d_reA12B-f-j_im-j_re-j_reA12B-k1_re-k2_re__bmin3_eta610_PL\PLval.pdf}
\end{figure}
\pagebreak
